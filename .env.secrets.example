# Secret variables are not required by easyROC app runtime today.
# Keep future secret keys in a separate untracked file (e.g. .env.secrets.local)
# or a platform secret manager and inject at deploy time.

